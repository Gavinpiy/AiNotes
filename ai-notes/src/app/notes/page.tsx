import { Metadata } from "next";

export const metadata: Metadata = {
  title: "AI-Notes - Home",
};

export default function NotesPage() {
  return (
    <div>
      <h1>Notes</h1>
    </div>
  );
}
